# name: CI

# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main, develop ]

# jobs:
#   backend:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup PHP
#         uses: shivammathur/setup-php@v2
#         with:
#           php-version: '8.4'
#           extensions: mbstring, xml, ctype, iconv, pdo_mysql, zip
#       - name: Cache Composer
#         uses: actions/cache@v4
#         with:
#           path: vendor
#           key: composer-${{ hashFiles('**/composer.lock') }}
#           restore-keys: composer-
#       - name: Install Dependencies
#         run: composer install --no-interaction --prefer-dist
#       - name: PHPCS Check
#         run: vendor/bin/phpcs --standard=.tooling/quality/phpcs.xml src
#       - name: PHPStan
#         run: vendor/bin/phpstan analyse --no-progress --configuration=.tooling/quality/phpstan.neon

#   frontend:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#       - name: Install Dependencies
#         run: npm ci
#       - name: Biome Linter
#         run: npx @biomejs/biome ci web
#       - name: TypeScript Check
#         run: npx tsc --noEmit
